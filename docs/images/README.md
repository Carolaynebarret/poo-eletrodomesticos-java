# Imagens

Esta pasta ainda não contém capturas de tela. Sugestões de imagens a produzir manualmente:

- Estrutura do projeto aberta no NetBeans (árvore `src/prova`), mostrando as classes `Equipamento`, `Ligavel`, `Computador`, `Consumo` e `Fogao`.
- Diagrama de classes (UML) ilustrando a relação de herança (`Computador`/`Fogao` estendem `Equipamento`) e de implementação da interface `Ligavel` (`Computador`/`Consumo`).
- Terminal executando `ant run` (ou `java -cp build/classes prova.Equipamento`) com o resultado da compilação/execução.

Nenhuma imagem foi gerada automaticamente — este arquivo é apenas um guia do que capturar.
